# 2026-06-16 — Calibrated gates, proficiency, and the first dry-run fixes

What shifted: the gate became adaptive, and the first round of dry-run findings (from the [2026-06-15 validation pass](2026-06-15-validation-apparatus.md)) was fixed and released as `updates/0003`.

- Added evidence: [optimal difficulty — the 85% rule and the ZPD](../evidence/reading/2026-06-16-optimal-difficulty-zpd.md) — grounds calibrated-challenge gates (bears on Q13, Q11).
- New registry practice **G24** (track proficiency to calibrate the gate); **G20/G16** revised for calibrated difficulty + override-explain-first; **G7** given a real surface (felt-vs-measured in the `session` periodic review). METHODOLOGY §3 (gates), §5/§6, §8 updated; provenance gains the `researcher-overridden` status in active use.
- The gate now **opens near the researcher's edge and adapts difficulty one question at a time** to hold the productive-struggle band — answering the D3 dry-run finding (expert-as-busywork; the Q13 risk, the reason G5 was retired). It is also a *stronger* bet for Q13: calibrated challenge is what the learning science says builds capability. The bar for "understood" is unchanged — only the path to it adapts.
- Released as **`updates/0003`** (all `permanent`): calibrated gates + proficiency (Item 1); researcher-override explain-first + `researcher-overridden` status (Item 2); push approval guard `.claude/settings.json` (Item 3); citations resolve-**and**-support in `gather`/`ideate` (Item 4); `state.md` decision-citation + the G7 felt-vs-measured surface (Item 5). Each item names the dry-test it closes. `starter_version` → 0.3.0.

Dry-run findings deliberately **not** acted on (maintainer call): the FRICTION research-content scrub was declined (friends-only dev phase, no privacy need); the over-stamping mechanical tripwire was declined in favor of the existing honesty prompt — *educate, don't enforce*.

Bears on Q13/Q14: the calibrated gate is the methodology's sharpened answer to the gate-as-busywork risk, and the D3 finding → this fix is a small instance of the validation apparatus steering the product *toward* the bet rather than away from it.
